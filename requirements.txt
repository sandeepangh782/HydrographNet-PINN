mlflow>=2.1.1
hydra-core
wandb
termcolor>=2.1.1
# PyTorch and PyTorch Geometric should be installed via conda:
# conda install pytorch torchvision torchaudio cpuonly -c pytorch
# conda install pyg -c pyg
